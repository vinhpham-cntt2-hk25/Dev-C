#include <stdio.h>
#include <math.h>
int main(){
	int number = 12345, number1, number2, Sum ;
	number1 = number % 2;
	number2 = number / 2;
	Sum = number1 + number2;
	printf ("Tong cua hai so la: %d", Sum);
	return 0;
	 
	
} 
