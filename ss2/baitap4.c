#include <stdio.h>
int main(){
    float Length = 10, Width = 5, Circumference, Area;
    Circumference  = (Length + Width) * 2;
	Area = Length * Width;
	printf ("Chu vi la : %f ", Circumference );
	printf ("Dien tich la : %f ", Area );
	return 0;
	 
} 
